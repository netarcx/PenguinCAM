# Core dependencies
ezdxf>=1.0.0
shapely>=2.0.0
Flask==3.0.0
flask-limiter>=4.0.0
PyYAML>=6.0
requests==2.31.0

# Imported directly, not just pulled in by Flask/requests. Both arrive transitively
# today, so nothing is broken - but a direct import that relies on another package's
# pin breaks silently the day that pin changes.
Werkzeug>=3.0.0      # frc_cam_gui_app: middleware.proxy_fix.ProxyFix
urllib3>=1.26.0      # onshape_integration: util.retry.Retry
gunicorn==21.2.0
google-auth==2.23.0
google-auth-oauthlib==1.1.0
google-auth-httplib2==0.1.1
google-api-python-client==2.100.0

# Testing dependencies
pygcode>=0.2.0
termcolor>=2.0.0
numpy>=1.24.0        # gcode_sim heightmap simulator
Pillow>=10.0.0       # gcode_sim golden/diff PNG rendering
fonttools>=4.50.0     # uploaded TTF/OTF outlines -> engraving polylines

# Local STEP -> depth-layered DXF conversion. The no-VTK build keeps the container
# smaller; PenguinCAM needs Open CASCADE geometry/topology, not its 3D viewer.
cadquery-ocp-novtk==7.9.3.1.1
